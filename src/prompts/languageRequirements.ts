/**
 * 全域語言要求。
 *
 * 背景：本系統所有面向使用者的法律文件一律使用繁體中文，但早期各提示詞
 * 沒有寫明這項要求，模型便偶爾以簡體中文回覆。實測曾出現精修產出含
 * 「费」等簡體字，交付閘門（generatedDocumentPipeline）因此擋下交付，
 * 使該功能恆定失敗。
 *
 * 這裡集中成單一常數，避免每個提示詞各寫一句而漏改。
 */
export const TRADITIONAL_CHINESE_REQUIREMENT =
  '全部輸出一律使用繁體中文，簡體中文一律視為不合格輸出。';

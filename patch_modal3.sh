#!/bin/bash
sed -i 's/<div className="text-xs text-rose-300 mt-2 font-medium">/<div className="flex justify-end mt-3">\n                          <button\n                            onClick={() => {\n                              const answers = (window as any).__syllogismAnswers || {};\n                              const appended = Object.values(answers).filter(Boolean).join("\\n");\n                              if (appended) {\n                                setSearchQuery(searchQuery + "\\n補充說明：\\n" + appended);\n                                setShowAiTriageModal(false);\n                                setTimeout(() => handleRunAiTriage(searchQuery + "\\n補充說明：\\n" + appended), 300);\n                              }\n                            }}\n                            className="bg-rose-500 hover:bg-rose-400 text-white px-4 py-2 rounded-lg text-xs font-bold transition-colors shadow-md"\n                          >\n                            送出補充事實並重新分析\n                          <\/button>\n                        <\/div>\n                        <div className="text-xs text-rose-300 mt-2 font-medium">/g' src/components/LegalGuideHome.tsx
